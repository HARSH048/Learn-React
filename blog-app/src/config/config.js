const config = {
  APPWRITE_URL: String(import.meta.env.APPWRITE_URL),
  APPWRITE_PROJECTION_ID: String(import.meta.env.APPWRITE_PROJECTION_ID),
  APPWRTITE_DATABASE_ID: String(import.meta.env.APPWRTITE_DATABASE_ID),
  APPWIRITE_COLLECTION_ID: String(import.meta.env.APPWIRITE_COLLECTION_ID),
  APPWIRITE_BUCKET_ID: String(import.meta.env.APPWIRITE_BUCKET_ID),
};

export default config;
